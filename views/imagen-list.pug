extends layout

block content
  h2 Listado de Imágenes
  if imagenes.length
    ul
      each img in imagenes
        li
          img(src=`/${img.ruta}`, width="100")
          
          |  #{img.descripcion || `sin descripcion`}
  else
    p No hay imágenes para mostrar.
